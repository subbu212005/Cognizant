# Exercise 1: Implementing the Singleton Pattern

## Objective
Ensure only one instance of the Logger class exists throughout the application.

## Steps
- Create a singleton Logger class.
- Make the constructor private.
- Expose a static GetInstance() method.
- Verify both references point to the same object.

## Expected Result
The program prints log messages and confirms that both references are the same instance.
